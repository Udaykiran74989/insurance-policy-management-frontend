import { ArrowDownRight, ArrowRight, Check, ChevronDown, HeartHandshake, House, LockKeyhole, Shield, Sparkles, WalletCards } from 'lucide-react';
import { Link, useLocation, useParams } from 'wouter';
import { PublicLayout } from '@/layouts/PublicLayout';
import { Badge, EmptyState, ErrorState, LoadingState } from '@/components/ui-kit';
import { productApi } from '@/api/productApi';
import { friendlyError } from '@/api/client';
import { useEffect, useState } from 'react';
import type { Product } from '@/types';

export const sampleProducts: Product[] = [
  { id: 1, productName: 'Family Health Cover', productType: 'HEALTH', description: 'Hospitalisation cover for the people who matter most, with room to breathe.', coverageAmount: 1000000, basePremium: 12400, status: 'ACTIVE' },
  { id: 2, productName: 'Life, in full', productType: 'LIFE', description: 'A dependable financial cushion for the years and people ahead.', coverageAmount: 5000000, basePremium: 18500, status: 'ACTIVE' },
  { id: 3, productName: 'Everyday Drive', productType: 'VEHICLE', description: 'Thoughtful protection for your car, from small scrapes to big surprises.', coverageAmount: 800000, basePremium: 8600, status: 'ACTIVE' },
  { id: 4, productName: 'Away with confidence', productType: 'TRAVEL', description: 'Medical and trip protection that lets you focus on the next view.', coverageAmount: 2500000, basePremium: 3900, status: 'ACTIVE' },
];
const inr = (value: number) => new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 }).format(value);
function ProductCard({ product }: { product: Product }) { return <article className="group relative overflow-hidden rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 shadow-[var(--shadow-soft)] transition duration-300 hover:-translate-y-1"><div className="mb-7 flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-xl bg-[hsl(var(--secondary))] text-[hsl(var(--primary))]">{product.productType === 'HEALTH' ? <HeartHandshake size={21} /> : product.productType === 'LIFE' ? <Shield size={21} /> : product.productType === 'VEHICLE' ? <House size={21} /> : <Sparkles size={21} />}</div><span className="text-[10px] font-bold uppercase tracking-[.14em] text-[hsl(var(--muted-foreground))]">{product.productType}</span></div><h3 className="font-display text-xl font-bold">{product.productName}</h3><p className="mt-2 min-h-12 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{product.description}</p><div className="mt-6 flex items-end justify-between border-t border-[hsl(var(--border))] pt-5"><div><span className="block text-xs text-[hsl(var(--muted-foreground))]">From</span><strong className="font-display text-lg">{inr(product.basePremium)}<span className="font-sans text-xs font-normal text-[hsl(var(--muted-foreground))]"> / year</span></strong></div><Link href={`/plans/${product.id}`} className="grid h-10 w-10 place-items-center rounded-full bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))] transition group-hover:translate-x-1" data-testid={`link-plan-${product.id}`}><ArrowRight size={17} /></Link></div></article>; }
export function HomePage() { return <PublicLayout><main><section className="surface-grid relative overflow-hidden"><div className="mx-auto grid max-w-7xl items-center gap-14 px-5 pb-24 pt-20 lg:grid-cols-[1.02fr_.98fr] lg:px-8 lg:pb-32 lg:pt-28"><div className="animate-rise"><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3 py-1.5 text-xs font-bold text-[hsl(var(--primary))]"><span className="h-1.5 w-1.5 rounded-full bg-[hsl(var(--accent))]" />Insurance, without the fog</div><h1 className="max-w-2xl font-display text-[clamp(2.8rem,6vw,5.6rem)] font-extrabold leading-[.98] tracking-[-.065em] text-[hsl(var(--foreground))]">The calm behind your <span className="text-[hsl(var(--primary))]">brave days.</span></h1><p className="mt-7 max-w-lg text-lg leading-8 text-[hsl(var(--muted-foreground))]">Cover for health, life, travel and the everyday. Clear enough to understand today, dependable enough to count on tomorrow.</p><div className="mt-9 flex flex-wrap items-center gap-3"><Link href="/plans" className="inline-flex min-h-12 items-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-5 text-sm font-bold text-[hsl(var(--primary-foreground))]" data-testid="link-explore-plans">Explore protection <ArrowRight size={17} /></Link><a href="#how-it-works" className="inline-flex min-h-12 items-center gap-2 px-4 text-sm font-bold text-[hsl(var(--foreground))]">How it works <ArrowDownRight size={17} className="text-[hsl(var(--primary))]" /></a></div><p className="mt-7 flex items-center gap-2 text-xs text-[hsl(var(--muted-foreground))]"><LockKeyhole size={13} /> Your information stays yours. Always.</p></div><div className="relative animate-rise delay-2"><div className="absolute -right-5 -top-7 h-40 w-40 rounded-full bg-[hsl(var(--accent))]/35 blur-3xl" /><div className="relative rounded-[2rem] border border-white/60 bg-[#dbe8df] p-4 shadow-[0_24px_70px_rgba(22,69,66,.15)]"><div className="rounded-[1.5rem] bg-[hsl(var(--primary))] p-6 text-[hsl(var(--primary-foreground))]"><div className="flex items-center justify-between"><span className="text-xs font-bold uppercase tracking-[.15em] opacity-70">Your cover snapshot</span><Shield size={20} className="text-[hsl(var(--accent))]" /></div><div className="mt-14"><p className="text-sm opacity-70">Protected value</p><p className="mt-1 font-display text-5xl font-extrabold tracking-[-.06em]">₹ 50L</p></div><div className="mt-8 flex items-center justify-between border-t border-white/15 pt-4 text-xs opacity-75"><span>SecureLife Life, in full</span><span>Active</span></div></div><div className="flex items-center justify-between px-3 pb-1 pt-4 text-xs font-bold text-[#285e57]"><span className="flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-[#4caa81]" /> Policy is up to date</span><span>•••</span></div></div><div className="absolute -bottom-5 -left-6 rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-4 shadow-[var(--shadow-soft)]"><div className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-full bg-[#fff0d1] text-[#8b5b10]"><Check size={17} /></div><div><p className="text-xs text-[hsl(var(--muted-foreground))]">Claim settled</p><p className="font-display text-sm font-bold">₹ 18,500 · today</p></div></div></div></div></div></section><section id="plans" className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><div className="flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[hsl(var(--primary))]">Made for real life</p><h2 className="mt-3 max-w-xl font-display text-4xl font-extrabold tracking-[-.05em] sm:text-5xl">Choose the cover that lets you exhale.</h2></div><Link href="/plans" className="text-sm font-bold text-[hsl(var(--primary))]" data-testid="link-view-all-plans">View all plans <ArrowRight className="ml-1 inline" size={15} /></Link></div><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{sampleProducts.map((p) => <ProductCard key={p.id} product={p} />)}</div></section><section id="how-it-works" className="bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))]"><div className="mx-auto grid max-w-7xl gap-14 px-5 py-24 lg:grid-cols-[.8fr_1.2fr] lg:px-8"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[hsl(var(--accent))]">One less thing to worry about</p><h2 className="mt-4 font-display text-4xl font-extrabold tracking-[-.05em] sm:text-5xl">Simple by design. Present when it matters.</h2><p className="mt-5 max-w-md leading-7 opacity-75">From your first quote to a claim on a difficult day, SecureLife keeps the next step visible.</p></div><div className="grid gap-8 sm:grid-cols-3">{[['01', 'Tell us what matters', 'A few honest details help us find a sensible fit.'], ['02', 'Pick your peace of mind', 'Compare cover and cost in plain language.'], ['03', 'Keep moving', 'Manage your policy and claims from one quiet corner.']].map(([n, title, copy]) => <div key={n} className="border-t border-white/20 pt-5"><span className="font-mono text-sm text-[hsl(var(--accent))]">{n}</span><h3 className="mt-12 font-display text-xl font-bold">{title}</h3><p className="mt-3 text-sm leading-6 opacity-70">{copy}</p></div>)}</div></div></section><section className="mx-auto grid max-w-7xl items-center gap-12 px-5 py-24 lg:grid-cols-[1fr_1fr] lg:px-8"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[hsl(var(--primary))]">A better promise</p><h2 className="mt-3 max-w-lg font-display text-4xl font-extrabold tracking-[-.05em] sm:text-5xl">No paperwork maze. No small-print scavenger hunt.</h2></div><div className="grid gap-6">{[['01', 'Words you can use', 'We translate insurance language into decisions you can actually make.'], ['02', 'A human-sized portal', 'See your cover, premium and next steps without digging.'], ['03', 'Help when it is not convenient', 'Track claims and questions in one place, 24 hours a day.']].map(([n, t, c]) => <div className="flex gap-5 border-b border-[hsl(var(--border))] pb-6" key={n}><span className="font-mono text-xs text-[hsl(var(--primary))]">{n}</span><div><h3 className="font-display font-bold">{t}</h3><p className="mt-1 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{c}</p></div></div>)}</div></section><section id="faq" className="border-t border-[hsl(var(--border))] bg-[#edf0e8]"><div className="mx-auto max-w-3xl px-5 py-24 lg:px-8"><p className="text-center text-xs font-bold uppercase tracking-[.16em] text-[hsl(var(--primary))]">Questions, answered</p><h2 className="mt-3 text-center font-display text-4xl font-extrabold tracking-[-.05em]">Good to know.</h2><div className="mt-10 divide-y divide-[hsl(var(--border))]">{[['Can I change my coverage later?', 'Your needs change. Sign in to review your options or speak with our support team.'], ['How do claims work?', 'Start a claim from your portal, add the basics and follow its status as our team reviews it.'], ['Is this a real insurance company?', 'SecureLife is a fictional demo environment built to showcase a thoughtful insurance experience. No real policies or payments are issued.']].map(([q, a]) => <details className="group py-5" key={q}><summary className="flex cursor-pointer list-none items-center justify-between font-display font-bold">{q}<ChevronDown size={18} className="transition group-open:rotate-180" /></summary><p className="max-w-2xl pt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{a}</p></details>)}</div></div></section><div className="bg-[#f2dfb2] px-5 py-3 text-center text-xs font-semibold text-[#73531b]" data-testid="text-demo-notice">Demo environment · SecureLife is fictional. Do not enter real personal or payment information.</div></main></PublicLayout>; }

export function PlansPage() { const [type, setType] = useState('ALL'); const filtered = sampleProducts.filter((p) => type === 'ALL' || p.productType === type); return <PublicLayout><main className="mx-auto max-w-7xl px-5 py-16 lg:px-8 lg:py-24"><div className="max-w-2xl"><Badge tone="accent">Protection, made legible</Badge><h1 className="mt-5 font-display text-5xl font-extrabold tracking-[-.06em] sm:text-6xl">Find your kind of ready.</h1><p className="mt-5 text-lg leading-8 text-[hsl(var(--muted-foreground))]">Browse our sample plans. When you are ready to take the next step, create an account and we will do the maths with you.</p></div><div className="mt-12 flex flex-wrap gap-2">{['ALL', 'HEALTH', 'LIFE', 'VEHICLE', 'TRAVEL'].map((item) => <button key={item} onClick={() => setType(item)} className={`rounded-full px-4 py-2 text-xs font-bold ${type === item ? 'bg-[hsl(var(--primary))] text-white' : 'border border-[hsl(var(--border))] bg-[hsl(var(--card))] text-[hsl(var(--muted-foreground))]'}`} data-testid={`button-filter-${item.toLowerCase()}`}>{item}</button>)}</div><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{filtered.map((p) => <ProductCard key={p.id} product={p} />)}</div><div className="mt-12 rounded-2xl bg-[hsl(var(--secondary))] p-7"><p className="font-display text-xl font-bold">Not sure where to begin?</p><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">The premium calculator turns a few details into a clear starting point.</p><Link href="/customer/premium-calculator" className="mt-4 inline-flex text-sm font-bold text-[hsl(var(--primary))]">Open calculator <ArrowRight size={15} className="ml-2" /></Link></div></main></PublicLayout>; }

export function PlanDetailPage() { const { id } = useParams<{ id: string }>(); const [product, setProduct] = useState<Product | null>(sampleProducts.find((x) => x.id === Number(id)) || null); const [loading, setLoading] = useState(!product); const [error, setError] = useState(''); useEffect(() => { if (!product) productApi.get(id || '').then(setProduct).catch((e) => setError(friendlyError(e))).finally(() => setLoading(false)); }, [id, product]); if (loading) return <PublicLayout><div className="mx-auto max-w-7xl px-5 py-20"><LoadingState /></div></PublicLayout>; if (!product) return <PublicLayout><div className="mx-auto max-w-2xl px-5 py-20">{error ? <ErrorState message={error} /> : <EmptyState title="Plan not found" detail="This plan may have moved." action={<Link href="/plans">Back to plans</Link>} />}</div></PublicLayout>; return <PublicLayout><main className="mx-auto max-w-7xl px-5 py-16 lg:px-8 lg:py-24"><Link href="/plans" className="text-sm font-bold text-[hsl(var(--primary))]" data-testid="link-back-plans">← All plans</Link><div className="mt-10 grid gap-14 lg:grid-cols-[1fr_.75fr]"><div><Badge tone="accent">{product.productType}</Badge><h1 className="mt-5 max-w-2xl font-display text-5xl font-extrabold tracking-[-.06em] sm:text-6xl">{product.productName}</h1><p className="mt-6 max-w-xl text-lg leading-8 text-[hsl(var(--muted-foreground))]">{product.description}</p><div className="mt-10 flex flex-wrap gap-3"><Link href="/register" className="inline-flex min-h-12 items-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-5 text-sm font-bold text-white" data-testid="link-start-quote">Start your quote <ArrowRight size={17} /></Link><Link href="/login" className="inline-flex min-h-12 items-center rounded-lg border border-[hsl(var(--border))] px-5 text-sm font-bold" data-testid="link-plan-sign-in">Already a member</Link></div></div><div className="rounded-2xl bg-[hsl(var(--primary))] p-7 text-white shadow-[var(--shadow-soft)]"><p className="text-xs font-bold uppercase tracking-[.14em] text-[hsl(var(--accent))]">A clear starting point</p><div className="mt-16"><span className="text-sm opacity-70">Coverage up to</span><p className="mt-1 font-display text-4xl font-extrabold">{inr(product.coverageAmount)}</p></div><div className="mt-8 border-t border-white/20 pt-5"><span className="text-sm opacity-70">Estimated from</span><p className="mt-1 font-display text-2xl font-bold">{inr(product.basePremium)} <span className="font-sans text-sm font-normal opacity-70">/ year</span></p></div><p className="mt-8 text-xs leading-5 opacity-60">Final premium depends on the details you share. No payment is collected in this demo.</p></div></div><section className="mt-24 border-t border-[hsl(var(--border))] pt-12"><h2 className="font-display text-2xl font-bold">Built around the moments that matter</h2><div className="mt-7 grid gap-4 md:grid-cols-3">{['Simple eligibility questions', 'Transparent premium calculation', 'A portal that remembers the details'].map((x) => <div className="rounded-xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5" key={x}><Check className="text-[hsl(var(--primary))]" size={18} /><p className="mt-4 font-semibold">{x}</p></div>)}</div></section></main></PublicLayout>; }