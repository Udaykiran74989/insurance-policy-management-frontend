import { Link, useLocation } from 'wouter';
import { ArrowRight, Menu, X } from 'lucide-react';
import { useState, type ReactNode } from 'react';
import { Logo, Button } from '@/components/ui-kit';
import { useAuth } from '@/context/AuthContext';

export function PublicHeader() { const [open, setOpen] = useState(false); const [, setLocation] = useLocation(); const { session } = useAuth(); return <header className="relative z-20 border-b border-[hsl(var(--border))] bg-[hsl(var(--background))]/90 backdrop-blur"><div className="mx-auto flex h-[72px] max-w-7xl items-center justify-between px-5 lg:px-8"><Link href="/" data-testid="link-home"><Logo /></Link><nav className="hidden items-center gap-7 md:flex"><a href="/#plans" className="text-sm font-medium text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--primary))]">Plans</a><a href="/#how-it-works" className="text-sm font-medium text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--primary))]">How it works</a><a href="/#faq" className="text-sm font-medium text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--primary))]">Questions</a></nav><div className="hidden items-center gap-3 md:flex">{session ? <Button variant="outline" onClick={() => setLocation(session.role === 'ADMIN' ? '/admin/dashboard' : '/customer/dashboard')} data-testid="button-open-portal">Open portal <ArrowRight size={15} /></Button> : <><Link href="/login" className="px-3 py-2 text-sm font-semibold text-[hsl(var(--muted-foreground))]" data-testid="link-login">Sign in</Link><Link href="/register" className="inline-flex min-h-10 items-center rounded-lg bg-[hsl(var(--primary))] px-4 text-sm font-semibold text-[hsl(var(--primary-foreground))]" data-testid="link-register">Get covered <ArrowRight size={15} className="ml-2" /></Link></>}</div><button className="md:hidden" onClick={() => setOpen(!open)} aria-label="Toggle menu" data-testid="button-mobile-menu">{open ? <X /> : <Menu />}</button></div>{open && <div className="border-t border-[hsl(var(--border))] bg-[hsl(var(--background))] p-5 md:hidden"><div className="grid gap-4 text-sm font-semibold"><a href="/#plans" onClick={() => setOpen(false)}>Plans</a><a href="/#how-it-works" onClick={() => setOpen(false)}>How it works</a><a href="/#faq" onClick={() => setOpen(false)}>Questions</a><Link href={session ? '/customer/dashboard' : '/login'} onClick={() => setOpen(false)}>{session ? 'Open portal' : 'Sign in'}</Link></div></div>}</header>; }
export function PublicLayout({ children }: { children: ReactNode }) { return <div className="noise min-h-[100dvh] bg-[hsl(var(--background))]"><PublicHeader />{children}<footer className="border-t border-[hsl(var(--border))] bg-[#edf0e8]"><div className="mx-auto grid max-w-7xl gap-10 px-5 py-12 md:grid-cols-[1.5fr_1fr_1fr_1fr] lg:px-8"><div><Logo /><p className="mt-4 max-w-xs text-sm leading-6 text-[hsl(var(--muted-foreground))]">Insurance for the life you are building, explained like a human.</p><p className="mt-8 text-xs text-[hsl(var(--muted-foreground))]">© 2025 SecureLife. Fictional demo environment.</p></div><div><p className="mb-3 text-xs font-bold uppercase tracking-[.12em] text-[hsl(var(--muted-foreground))]">Explore</p><div className="grid gap-2.5 text-sm"><a href="/#plans">Plans</a><a href="/#how-it-works">How it works</a><Link href="/register">Get a quote</Link></div></div><div><p className="mb-3 text-xs font-bold uppercase tracking-[.12em] text-[hsl(var(--muted-foreground))]">Support</p><div className="grid gap-2.5 text-sm"><a href="/#faq">FAQs</a><a href="mailto:hello@securelife.demo">hello@securelife.demo</a><span>Mon–Fri, 9am–6pm IST</span></div></div><div className="rounded-xl bg-[hsl(var(--primary))] p-5 text-[hsl(var(--primary-foreground))]"><p className="font-display font-bold">A clearer kind of cover.</p><p className="mt-2 text-sm leading-5 opacity-80">No jargon, no pressure. Just a useful safety net.</p></div></div></footer></div>; }